apiVersion: v1
kind: Service
metadata:
  name: bonos-service-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: bonos-service
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004